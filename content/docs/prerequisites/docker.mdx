---
title: Docker Installation
description: Step-by-step guide to install Docker on Ubuntu, macOS, and Windows
---

import { Tabs, Tab } from 'fumadocs-ui/components/tabs';

This guide walks you through installing Docker on your operating system.

## Prerequisites

<Tabs groupId="os" items={['Ubuntu', 'macOS', 'Windows']}>
  <Tab value="Ubuntu">
**Ubuntu 22.04** server or desktop with a user account that has `sudo` privileges and terminal access.
  </Tab>
  <Tab value="macOS">
**macOS 12 (Monterey) or later** with at least 4GB of RAM. Works with Apple Silicon (M1/M2/M3) or Intel chips.
  </Tab>
  <Tab value="Windows">
**Windows 10 64-bit** (Pro, Enterprise, or Education, Build 19041+) or **Windows 11**. Requires WSL 2 (recommended) or Hyper-V, at least 4GB of RAM, and BIOS-level hardware virtualization enabled.
  </Tab>
</Tabs>

## Installation

<Tabs groupId="os" items={['Ubuntu', 'macOS', 'Windows']}>
  <Tab value="Ubuntu">
### Step 1: Update Package Index

```bash
sudo apt update
```

### Step 2: Install Required Packages

```bash
sudo apt install apt-transport-https ca-certificates curl software-properties-common
```

### Step 3: Add Docker's GPG Key

```bash
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg
```

### Step 4: Add Docker Repository

```bash
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
```

### Step 5: Install Docker

```bash
sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io
```

### Step 6: Run Docker Without Sudo (Recommended)

```bash
sudo usermod -aG docker ${USER}
```

Log out and log back in for changes to take effect.
  </Tab>
  <Tab value="macOS">
### Option A: Docker Desktop (Recommended)

Download [Docker Desktop for Mac](https://www.docker.com/products/docker-desktop/). Choose **Apple Silicon** or **Intel chip** based on your Mac. Open the `.dmg` file, drag Docker to Applications, then launch Docker and accept the service agreement.

### Option B: Using Homebrew

```bash
brew install --cask docker
```

Then launch Docker from Applications.

> For the full official guide, see the [Docker Desktop for Mac documentation](https://docs.docker.com/desktop/setup/install/mac-install/).
  </Tab>
  <Tab value="Windows">
### Step 1: Enable WSL 2

Open PowerShell as Administrator:

```powershell
dism.exe /online /enable-feature /featurename:Microsoft-Windows-Subsystem-Linux /all /norestart
dism.exe /online /enable-feature /featurename:VirtualMachinePlatform /all /norestart
```

Restart your computer, then run:

```powershell
wsl --set-default-version 2
```

Download and install the [WSL 2 Linux kernel update](https://aka.ms/wsl2kernel).

### Step 2: Install Docker Desktop

Download [Docker Desktop for Windows](https://www.docker.com/products/docker-desktop/). Run the installer and select **Use WSL 2 instead of Hyper-V**. Follow the installation wizard, then launch Docker Desktop from the Start menu.

> For the full official guide, see the [Docker Desktop for Windows documentation](https://docs.docker.com/desktop/setup/install/windows-install/).
  </Tab>
</Tabs>

## Verify Installation

<Tabs groupId="os" items={['Ubuntu', 'macOS', 'Windows']}>
  <Tab value="Ubuntu">
```bash
sudo systemctl status docker
docker --version
docker run hello-world
```
  </Tab>
  <Tab value="macOS">
```bash
docker --version
docker run hello-world
```
  </Tab>
  <Tab value="Windows">
```powershell
docker --version
docker run hello-world
```
  </Tab>
</Tabs>

## Troubleshooting

<Tabs groupId="os" items={['Ubuntu', 'macOS', 'Windows']}>
  <Tab value="Ubuntu">
### Permission Denied Error

```bash
# Verify docker group membership
groups ${USER}

# Re-add to docker group if needed
sudo usermod -aG docker ${USER}

# Log out and back in
```

### Docker Service Not Starting

```bash
sudo systemctl status docker
sudo journalctl -u docker.service
```

### GPG Key Issues

```bash
sudo rm /usr/share/keyrings/docker-archive-keyring.gpg
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg
```
  </Tab>
  <Tab value="macOS">
### Docker Desktop Won't Start

Check virtualization support:

```bash
sysctl -a | grep machdep.cpu.features
```

Try resetting: Docker icon → Troubleshoot → Reset to factory defaults

### Permission Issues

```bash
sudo chown "$USER":staff ~/.docker -R
sudo chmod g+rwx ~/.docker -R
```

### Apple Silicon Performance

Enable Rosetta for x86 images: Docker Desktop Settings → Features in development → Use Rosetta for x86/amd64 emulation
  </Tab>
  <Tab value="Windows">
### WSL 2 Issues

```powershell
wsl --status
wsl --update
wsl --list --verbose
```

### Virtualization Not Enabled

Enter BIOS/UEFI settings during boot and enable Intel VT-x or AMD-V.

### Permission Errors

```powershell
# Run as Administrator
net localgroup docker-users "YOUR_USERNAME" /add
```

Log out and back in.

### Slow Performance

Create `%USERPROFILE%\.wslconfig`:

```
[wsl2]
memory=4GB
processors=2
```

Then restart WSL: `wsl --shutdown`
  </Tab>
</Tabs>

## Useful Commands

| Command | Description |
|---------|-------------|
| `docker ps` | List running containers |
| `docker ps -a` | List all containers |
| `docker images` | List downloaded images |
| `docker pull <image>` | Download an image |
| `docker run <image>` | Run a container |
| `docker stop <container>` | Stop a container |
| `docker rm <container>` | Remove a container |
| `docker system prune` | Clean up unused resources |
